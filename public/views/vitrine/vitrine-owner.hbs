{{#if this.showButtonEdit}}
  {{!-- MODAL --}}
  <form action="/vitrine-download" method="post" class="upload-dom" enctype="multipart/form-data">
    <input type="file" name="photo-vitrine" id="photo">
    <button type="submit">SAVE</button>
    <div class="exit-upload-dom"> <span>X</span> </div>
  </form>
  {{!-- MODAL --}}
{{/if}}

<main id="main-owner-tutorial">
  {{!-- userInformation é só o article --}}
  {{!-- userInformation.userId é a informação do article do usuario --}}
  <nav class="nav-tutorial">

    <img src="../images/Mixer-logo-02.png" alt="logo" height="100">
    <ul class="d-none d-md-inline-flex">
      <li> <a href="/">Home</a></li>
      <li> <a href="/vitrine">Projects</a></li>
      <li> <a href="/tutorial">Tutorials</a> </li>
      <li class = "lgs"> <a href="/login">Login</a> </li>
      <li class = "lgs"> <a href="/sign">Signup</a> </li>

    </ul>

  </nav>


  <div data-aos="fade-right" class="user-main-info">
    <div class="user-icon">
      <img src="{{Project.userId.image}}" alt="user_icon" class="image-switch">
    </div>
    <div class="tutorial-h1-flex">
      <h1>{{Project.title}}</h1>
      {{#if this.showButtonEdit}}
      <div class="remove-and-edit-tutorial">
      <form action="/vitrine/remove?id={{Project._id}}" method="POST">
        <button type="submit" class="button-remove-owner exclusive-button-remove">
          <p>Remove</p>
        </button>
      </form>
      <button type="submit" class="button-edit-owner">
        <a href="/vitrine/editView?id={{Project._id}}">Edit</a>
      </button>
    </div>
    {{/if}}
  </div>
  </div>
  <div data-aos="fade-right" class="tutorial-text-flex view-erase">
    <div class="tutorial-text-bg">
      <p>{{Project.text}}</p>
      <p class="owner"><span>Owner: </span>{{Project.userId.name}}</p>
      <button id="comments-view-vitrine">
        <p>Comments</p>
        </butt>
    </div>
  </div>

  {{!-- quando o cara vem pelo create, não pelo tutorial all --}}
  {{#if Article}}
  <h1>{{this.title}}</h1>
  <h1>{{this.text}}</h1>
  <span>{{this.name}}</span>
  {{/if}}

  {{!-- caso error, recebera o error --}}
  {{#if error}}
  <h1>{{error}}</h1>
  {{/if}}


  {{!-- propiedades do tutorial criado --}}
  {{!-- <h1>{{Project}}</h1>
<span>{{likes}}</span> --}}
  {{!-- caso error, recebera o error --}}
  {{#if error}}
  <h1>{{error}}</h1>
  {{/if}}


  {{!-- <section id="section-comments">
    <button class="exit-comments">EXIT</button>

    <h2 id="h2-comment-tile"></h2>
    <div class="div-comments"></div>

    <form action="/comments-tutorial/create?id={{Project._id}}" method="post">
      <textarea name="text" id="text-comment" cols="100" rows="6"></textarea>
      <button type="submit" id="div-comments-create"> COMMENTE VOCE TAMBEM </button>
    </form>

  </section> --}}

  <section id="section-comments">
    <button class="exit-comments">x</button>

    <h2 id="h2-comment-tile"></h2>
    <article id="comment-box" style="overflow-y:scroll; height:30%;">
      <div class="div-comments"></div>
    </article>

    <form class='comment-space' action="/comments-project/create?id={{Project._id}}" method="post">
      <textarea name="text" id="text-comment" cols="100" rows="6" placeholder="write a comment..."></textarea>
      <button type="submit">
        <p class="comment">COMMENT</p>
      </button>
    </form>

  </section>

  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>